# KN Win32 API Monitor v0.1.0.3

This patch hardens session identity and shared-memory transport lifecycle after adversarial review of the native capture path.

## Highlights

- Operation, writer, and daemon IDs no longer collide within the same millisecond (helper + Tauri).
- Shared-memory transport creation fails closed when a mapping name already exists.
- Threaded shared-transport reader destructor no longer risks process abort on join timeout.
- Agent transport open validates header size and capacity bounds.
- Transport producer CAS failures publish a poison slot instead of permanently stalling the consumer.
- Named cancellation events are reset on open so stale signals cannot cancel a new operation.
- Transport record sequence is loaded and stored with interlocked 64-bit operations for x86 safety.
- Release packaging supports single-config native output layouts (Ninja/NMake).

## Validation

- Native Release build
- Tauri desktop Release build (`knmon-ui.exe`)
- `knmon-collector smoke-backpressure`
- `knmon-collector smoke-shared-transport-reader`
- `knmon-collector smoke-threaded-session-reader`
- Staged package helper `list-targets`
- Staged package collector shared-transport reader smoke
- Release package layout includes `knmon-ui.exe`, native helper/agent/collector, sample targets, `LICENSE`, `README.md`, `VERSION`, and `BUILD-INFO.json`

## Package

- Asset: `KnWin32ApiMonitor-0.1.0.3-Release.zip`
- Entry point after extraction: `knmon-ui.exe`
- Architecture: x64 native monitor package
- Requires Windows 10/11 and WebView2 Runtime

## SHA256

`C49C485CF2F6D4A84860B043F39F859D035723DF8030D1D34A769F02A6C77933`
