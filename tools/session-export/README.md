# session-export

Future command-line exporter for `.knapm` sessions.

Phase 1 exports JSONL directly from the UI mock event model. This directory reserves the tool boundary for later conversion from `.knapm` session chunks to JSONL, CSV, SQLite, or Parquet.

