# knmon-agent32 source placeholder

The first live controlled launch path builds the x64 agent. This source folder reserves the x86 agent implementation area for the later Win32 generator/toolchain pass.

