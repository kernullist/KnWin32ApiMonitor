# knmon-agent32

Future x86 agent DLL project placeholder.

The first native-capture goal builds the x64 early-bird APC agent and leaves this x86 area as an explicit skeleton. A later Win32 generator/toolchain pass should add the x86 DLL target, exact WOW64 controller checks, and cross-architecture refusal tests.
